/**
 * Global JavaScript utilities for Shopify theme
 */

// Add no-js class replacement (already done in layout but ensure it's early)
document.documentElement.className = document.documentElement.className.replace('no-js', 'js');
